{
    "mcpServers": {
        "image_search": {
            "type": "http",
            "url": "${ZCODE_BASE_URL}/api/v1/mcp/server/image_search",
            "auth": {
                "type": "zcode_official",
                "provider": "jwt_token"
            },
            "timeoutMs": 90000
        }
    }
}
